# =============================================================================
# Galaxy Classifier — CHANCES Project
# Runtime dependencies
# =============================================================================

# ---- Core scientific stack --------------------------------------------------
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# ---- Deep learning ----------------------------------------------------------
torch>=2.1          # PyTorch (install the CUDA variant manually if needed)
pytorch-lightning>=2.2

# ---- Zoobot ecosystem (install in this order) -------------------------------
# See https://github.com/mwalmsley/zoobot for full installation instructions.
# GPU users: install the matching torch+cuda wheel first, then:
#   pip install zoobot[pytorch] galaxy-datasets
zoobot[pytorch]>=3.0
galaxy-datasets>=0.0.9

# ---- Utilities --------------------------------------------------------------
tqdm>=4.66
Pillow>=10.0
requests>=2.31
